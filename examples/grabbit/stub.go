package main

import "go.bbkane.com/warg/command"

// Stub code, so ignoring "no package level vars; no func init" guideline
var version = ""

var editConfig = command.DoNothing
var grab = command.DoNothing
