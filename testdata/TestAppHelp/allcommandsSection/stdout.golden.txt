Usage:

  grabbit config [command]

Change grabbit's config

Commands:

  edit   Edit the config. A default config will be created if it doesn't exist

Use "grabbit config [command] --help" for more information about a command.

Footer:
Examples:

	# Grab without config
	grabbit grab

	# Edit config, then grab
	grabbit config edit
	grabbit grab
	
